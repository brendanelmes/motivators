{
  "name": "motivators",
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently \"cd ./backend && ./gradlew bootRun\" \"cd ./frontend && npm run start\"",
    "test": "concurrently \"cd ./backend && ./gradlew test\" \"cd ./frontend && npm run test\"",
    "prepare": "husky install"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^7.3.0",
    "husky": "^8.0.0",
    "pretty-quick": "^3.1.3"
  }
}
